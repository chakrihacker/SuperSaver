<%= form_with(model: conversation_membership, local: true) do |form| %>
  <% if conversation_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation_membership.errors.count, "error") %> prohibited this conversation_membership from being saved:</h2>

      <ul>
        <% conversation_membership.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :conversation_id %>
    <%= form.text_field :conversation_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
